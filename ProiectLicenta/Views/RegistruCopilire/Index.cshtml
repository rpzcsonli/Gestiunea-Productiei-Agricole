@using ProiectLicenta.Models
@model List<RegistruCopilire>
    @{
    ViewData["Title"] = "Registru Copilire";
}

    <h1 class="fw-bold pb-2 pb-xl-3 text-center"> Registru Copilire </h1>

<table class="mb-3">
    <tr>
        <th> <input class="btn btn-warning" type="button" value="Inapoi" onclick="@("window.location.href='" + @Url.Action("Index", "Home") + "'");" /></th>
        <th> <a class="btn btn-primary" asp-controller="RegistruCopilire" asp-action="Adaugare"> Adauga Copilire</a></th>
        <th>    <a class="btn btn-primary" asp-controller="RegistruCopilire" asp-action="Stergere"> Sterge Copilire</a></th>
    </tr>
</table>
    <div>
@if (Model.Count == 0)
{
    <p> Nu exista copiliri in registru!</p>
}
else
{
    <table class="table table-striped table-bordered">
        <tr>
            <th>
                Cod Copilire
            </th>
            <th>
                Parcela
            </th>
            <th>
                Numar Plante
            </th>
            <th>
                Angajat
            </th>
            <th>
                Data Copilire
            </th>
        </tr>
        @foreach (var var in Model)
        {
            <tr>
                <td>
                    @var.CodCopilire
                </td>
                <td>
                    @var.Parcela.CodParcela | @var.Parcela.Locatie
                </td>
                <td>
                    @var.NumarPlante
                </td>
                <td>
                    @var.Angajat.CodAngajat | @var.Angajat.Nume @var.Angajat.Prenume
                </td>
                <td>
                        @var.DataCopilire.ToString("dd/MM/yyyy")
                </td>
            </tr>
        }
    </table>
}
 </div>
