@model List<RegistruPalisare>
@{
    ViewData["Title"] = "Stergere Palisare";
}
<h1 class="  fw-bold pb-2 pb-xl-3 text-center"><i class="bi bi-trash3"></i> Sterge Palisare</h1>
<a class="btn btn-warning me-3" href="@Url.Action("Index", "RegistruPalisare")"><i class="bi bi-arrow-left"></i> Inapoi</a>
<div class="mb-3 mt-3">
    <form class="d-flex" asp-action="Stergere" method="get">
        <button class="btn btn-outline-success" type="submit"><i class="bi bi-search"></i></button>
        <input class="form-control ms-2" type="search" placeholder="Cautare" aria-label="Cautare" name="searchString" value="@ViewBag.CurrentFilter">
    </form>
</div>
<form method="post">
    <div class="mt-3">
        @if (Model.Count == 0)
        {
            <p>Nu exista Palisari!</p>
        }
        else
        {
            <table class="table table-striped table-bordered">
                <tr>
                    <th colspan="2" class="fit-content">
                        Nr.
                    </th>
                    <th>
                        Parcela
                    </th>
                    <th>
                        Numar Plante
                    </th>
                    <th>
                        Angajat
                    </th>
                    <th>
                        Data Palisare
                    </th>
                </tr>
                @foreach (var var in Model)
                {
                    <tr>
                        <td>
                            <input type="checkbox" name="registruPalisareId" value="@var.CodPalisare" />
                        </td>
                        <td>
                            @var.CodPalisare
                        </td>
                        <td>
                            @var.Parcela.Locatie

                        </td>
                        <td>
                            @var.NumarPlante
                        </td>
                        <td>
                            @var.Angajat.Nume @var.Angajat.Prenume
                        </td>
                        <td>
                            @var.DataPalisare.ToString("dd/MM/yyyy")
                        </td>
                    </tr>
                }
            </table>
        }
    </div>
    <input value="Stergere" type="submit" name="Stergere" class="btn btn-danger" />
</form>


